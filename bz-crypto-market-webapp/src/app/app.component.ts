import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styles: []
})
export class AppComponent {
  title = 'bz-crypto-market-webapp';
  // tickers: Ticker[] = [new Ticker(0, "Coingecko"), new Ticker(1, "Binance")];
  // selectedTicker: Ticker = this.tickers[0];
}
